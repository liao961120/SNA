Converted to csv with UCINET.

The filenames match the files in `../ucinet_data/`.


The scripts `rename.*` are used to unify different names of the same department. The output data is appended with `2` in `../`.
